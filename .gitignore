/target
/tmp
/temp
/other
/dist
.idea
/logs
.zcode
.claude
# Machine-local Claude permissions are never tracked.
/.claude/settings.local.json
tests/sse_e2e/results
__pycache__/
/target-console
/target-unstable
docker/app-runtime-base/downloads/

.classpath
project_workspace
.settings
.checkstyle
.checkstyle_suppressions.xml
.DS_Store
docker/rcoder
.vscode
docker/rcoder-agent-runner/bin/agent_runner
docker/rcoder-agent-runner/downloads
docker/computer-cache
docker/computer-project-workspace
docker/userapp-workspace
docker/logs

# Stakpak session files
.stakpak/session*
crates/agent_runner/config.yml

# npm package binaries (generated during CI)
npm/*/rcoder-cli
npm/*/rcoder-cli.exe

docker/app-workspace/*
docker/rcoder-agent-runner/bin/*

# Ignore DevSpace cache and log folder
.devspace/
target.garbage/
.workbuddy/
crates/*/target/
.qoder/

# 模型探活集成测试本地配置(含 API key,不提交)
*.env.local
tests/sse_e2e/results/
tests/userapp_e2e_local/
tests-e2e/reports/
target-console/debug/build/ahash-8c244d12b1e0d55d/invoked.timestamp
target-console/
target-unstable/
target-splitcheck/
target-tssync/
# UserApp control storage (database, WAL, SHM and instance lock).
/data/rcoder/
/docker/data/

# Isolated remote build receipts, tools, locks, and source manifests.
/.remote-k8s/
target-check/
/target-appcli/
target_DIR_PLACEHOLDER/

# 内部工作文档（spec/plan/tasks/verification、问题记录、handoff 等）仅本地保留，不随仓库发布。
# 位置仍为 ./specs/，本地开发流程不变；新文件不会误提交。
/specs/

# UserApp 错误页 dev 运行产物（上传的运行时页面，不入库）
docker/error-pages/
