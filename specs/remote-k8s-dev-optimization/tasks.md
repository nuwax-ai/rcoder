# 实施与验收任务

所有框表示待完成；本文件不是既有成果清单。

## A. 现状与固定快照

- [x] 检查 git status、相关diff、AGENTS和启动器调用链；记录已有改动，保护业务代码。
- [ ] 记录当前构建各阶段、测试准备与实际执行耗时，不预设优化倍数。
- [x] 定义source/test/镜像/receipt版本契约及旧记录兼容行为；旧记录缺字段时安全cache miss或要求重新验证。
- [x] 实现独立测试快照与启动器显式输入/报告目录，保留旧Compose行为。
- [ ] verify构建与测试使用同轮输入；测试期间继续编辑活动目录不失败。
- [x] 覆盖未提交/新增/删除、执行位、合法链接、逃逸链接、复制竞争、凭据排除。
- [x] 覆盖冻结目录输入被修改/新增/删除时失败，输出目录不污染指纹。
- [ ] 所有权、部署身份、环境锁丢失、中断清理仍有效；测试不会加载活动目录脚本/fixture。

## B. 状态诊断

- [x] status只读展示身份、入口、实时状态和历史测试来源。
- [x] check分层、有预算；缺权限为unknown而非pass。
- [x] logs逐项收集，保存失败详情，原错误不被诊断错误覆盖。
- [ ] 实测节点/Gateway/直连；不自动改集群全局配置、接受资源漂移或删PVC。

## C. 构建缓存

- [x] 相同完整有效输入复用成功digest，失败/半成品记录不会命中。
- [x] 基础镜像/工具链/锁文件/feature/profile/脚本变化失效。
- [x] registry artifact不存在触发重建，权限/网络失败明确报告。
- [x] 记录 reused_from、cache_key、来源摘要和耗时，区分复用与重新编译。
- [ ] 实施并验证镜像目标级输入闭包；未知依赖保守重建。
- [ ] 证明文档与真实构建输入分离，include_str/build.rs引用文件不漏判。
- [ ] 仅有实测证据后优化全源touch；验证二进制确实反映修改，而非只看Docker缓存日志。

## D. 场景筛选和重跑

- [x] Chat场景可枚举、筛选；空/未知/非法组合提前拒绝。
- [x] Make参数安全传递，包含空格、引号、$()等输入的测试不执行shell片段。
- [ ] UserApp依赖链保持完整；未拆独立fixture前拒绝任意步骤过滤。
- [ ] retest使用父报告测试快照，部署身份变化拒绝；没有失败case/基础设施失败时清晰报错。
- [ ] 重跑产生新报告，父报告不修改；部分成功不计全套成功。

## E. 真实交付

- [ ] Python工作流、原E2E启动器、UserApp启动器与Make回归通过，记录命令及退出码。
- [ ] 原Compose入口无回归；涉及启动器共享逻辑时执行适当Compose场景。
- [ ] 个人K8s smoke/gateway通过；实际执行userapp与真实LLM chat，失败单列归因。
- [ ] 验证测试进行中继续编辑活动目录；不修改被测快照；最终结果仍能对应固定输入。
- [ ] 相同输入重复verify缓存命中；源文件/依赖变化重建；保存前后耗时。
- [ ] 受控失败验证筛选与重跑报告，不能伪造真实AI成功。
- [ ] 更新AGENTS、README、配置示例及verification.md；保留生产发布验收边界。

## 后续可选

- [ ] watch防抖与最新快照队列；只在必做部分稳定后实施，AI运行显式选择。
