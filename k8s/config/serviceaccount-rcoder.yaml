# rcoder 主服务 ServiceAccount（app_manager / Deployment 创建者，绑定 rcoder-role）。
# 单文档（见 rbac.yaml 顶部说明）。rcoder-pods-sa 见 serviceaccount.yaml。
apiVersion: v1
kind: ServiceAccount
metadata:
  name: rcoder-sa
  namespace: rcoder-dev
