# Kind 集群配置
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: rcoder-dev
nodes:
- role: control-plane
  extraPortMappings:
  # RCoder 服务端口映射
  - containerPort: 30080
    hostPort: 8080
    protocol: TCP
