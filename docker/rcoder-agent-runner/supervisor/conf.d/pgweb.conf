# pgweb（PostgreSQL Web UI；--sessions 启动不连 PG，UI 手填，不依赖 PG ready）
[program:pgweb]
command=/usr/local/bin/pgweb --bind=0.0.0.0 --listen=%(ENV_PGWEB_PORT)s --sessions
priority=20
autostart=true
autorestart=true
startsecs=3
stdout_logfile=/app/logs/pgweb.out.log
stderr_logfile=/app/logs/pgweb.err.log
