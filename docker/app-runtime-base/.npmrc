# npm 默认镜像源 —— 国内加速。
# 作用范围：① 镜像构建时 Dockerfile.node 的 `npm install -g`；
#          ② UserApp 运行时 start.sh 的 `npm install`（每个 app 启动装依赖）。
# 如需切回官方源，覆盖此文件或传 NPM_CONFIG_REGISTRY 环境变量。
registry=https://registry.npmmirror.com
