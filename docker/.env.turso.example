# Optional: absolute, local filesystem path dedicated to this Compose project.
# Default: ./data/rcoder relative to docker-compose.yml.
# RCODER_DATA_DIR=/absolute/path/to/rcoder-data
RCODER_USERAPP_ENSURE_TIMEOUT_SECONDS=90
