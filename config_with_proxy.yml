# rcoder 配置文件 - 启用代理服务
default_agent: Codex
projects_dir: ./project_workspace
port: 3000

# Pingora 代理配置
proxy_config:
  listen_port: 8080
  default_backend_port: 3000
  backend_host: "127.0.0.1"
  port_param: "port"